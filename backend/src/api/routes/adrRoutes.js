const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented with ADR functionality
router.get('/', (req, res) => {
  res.json({ status: 'success', message: 'ADR routes - coming soon' });
});

module.exports = router;

// Made with Bob
