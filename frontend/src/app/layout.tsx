import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'AI Code Anthropologist | IBM',
  description: 'Analyze GitHub repositories and generate comprehensive architectural documentation using IBM AI',
  keywords: ['AI', 'Code Analysis', 'Architecture', 'Documentation', 'IBM', 'GitHub'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}

// Made with Bob
